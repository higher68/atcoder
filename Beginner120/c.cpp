#include <bits/stdc++.h>
using namespace std;
string S;
int sol=0;
void calc(sol){
    judge = 0;
    for(int i=1;i<strlen(S.c_str());i+=2){
        pre = int(S[i-1]);
        back = int(S[i]);
        if(abs(back-pre))
    }

}
int main(void){
    cin >> S;
    calc(S)
    return 0;
}