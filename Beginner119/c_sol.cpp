#include <bits/stdc++.h>
using namespace std;oct

void dfs(int depth, vector<int> v){
    if(depth=N) calc(v)
    for(int i=0;i<<N;i++){
        v[depth]=i;
        dfs(depth+1, v);
    }
}

int main(void) {
